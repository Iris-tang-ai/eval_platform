# 评测任务管理系统 PRD

## 1. 产品概述

### 1.1 产品定位
评测任务管理系统是一个内部使用的Web应用，用于管理和追踪评测任务的整个生命周期，提供任务创建、列表展示、状态追踪等核心功能。

### 1.2 目标用户
- 评测团队工程师
- 项目管理人员
- 质量保障团队

### 1.3 核心价值
- 统一管理评测任务，提升团队协作效率
- 清晰展示任务历史，便于回溯分析
- 规范化评测流程，确保质量可控

---

## 2. 功能需求

### 2.1 任务列表展示

#### 2.1.1 功能描述
展示所有评测任务的历史记录，支持查看任务详情、状态筛选和搜索。

#### 2.1.2 数据字段
| 字段名 | 类型 | 说明 | 是否必填 |
|--------|------|------|----------|
| 任务ID | string | 唯一标识符，自动生成 | 是 |
| 任务名称 | string | 任务描述性名称 | 是 |
| 任务类型 | enum | 大模型评测/图像评测/语音评测/视频评测 | 是 |
| 创建人 | string | 任务创建者姓名 | 是 |
| 创建时间 | datetime | 任务创建时间戳 | 是 |
| 状态 | enum | 待执行/执行中/已完成/已失败/已取消 | 是 |
| 优先级 | enum | 高/中/低 | 是 |
| 描述 | string | 任务详细描述 | 否 |

#### 2.1.3 界面交互
1. **列表展示**
   - 采用卡片式布局，每个任务一张卡片
   - 卡片头部显示任务名称和状态标签
   - 卡片主体显示任务类型、创建人、创建时间、优先级
   - 状态使用不同颜色标签区分：
     - 待执行：灰色
     - 执行中：蓝色
     - 已完成：绿色
     - 已失败：红色
     - 已取消：黄色

2. **状态筛选**
   - 顶部提供状态筛选标签页：全部/待执行/执行中/已完成/已失败
   - 点击标签页即时切换列表内容
   - 默认展示"全部"状态

3. **搜索功能**
   - 顶部搜索框支持按任务名称模糊搜索
   - 输入即时过滤，无需点击搜索按钮
   - 搜索框支持清空功能

4. **列表排序**
   - 默认按创建时间倒序排列（最新任务在前）
   - 支持按优先级、状态排序

#### 2.1.4 验收标准
- [ ] 任务列表正确展示所有历史任务
- [ ] 状态筛选功能正常，点击标签页能正确过滤
- [ ] 搜索功能正常，输入关键词能正确过滤
- [ ] 排序功能正常，默认按时间倒序
- [ ] 空状态友好提示"暂无任务数据"
- [ ] 任务卡片信息完整，无遗漏字段

---

### 2.2 新建任务

#### 2.2.1 功能描述
提供新建评测任务的入口，支持两种创建模式：
- **手工创建**：用户手动填写表单创建单个任务
- **自然语言创建**：用户输入自然语言描述，系统自动拆解生成多个任务

#### 2.2.2 界面布局

**展示方式**：全屏对话框（Dialog）
- 点击"新建任务"按钮后，全屏展示创建界面
- 背景带半透明遮罩层
- 点击遮罩层或右上角关闭按钮可关闭

**页面结构**：
```
┌─────────────────────────────────────────────────────────┐
│  新建任务                                        [×]   │
├─────────────────────────────────────────────────────────┤
│  [手工创建]  [自然语言创建]                             │
├─────────────────────────────────────────────────────────┤
│                                                          │
│                    内容区域                              │
│            （根据模式显示不同内容）                       │
│                                                          │
│                                                          │
├─────────────────────────────────────────────────────────┤
│                              [取消]  [创建/确认创建]    │
└─────────────────────────────────────────────────────────┘
```

**尺寸规范**：
- 宽度：最大800px，居中显示
- 高度：自适应内容，最大90vh
- 内边距：32px

#### 2.2.3 创建模式选择

**模式切换方式**：
- 打开新建任务面板时，顶部显示两个模式选项卡
- 默认选中"手工创建"模式
- 点击切换即时切换，无需刷新

**模式说明**：
| 模式 | 图标 | 说明 | 适用场景 |
|------|------|------|----------|
| 手工创建 | ✏️ 表单图标 | 手动填写表单创建单个任务 | 明确知道任务细节，创建单个任务 |
| 自然语言创建 | 🤝 AI图标 | 输入自然语言，AI自动拆解生成多个任务 | 有整体需求描述，需要拆解成多个子任务 |

#### 2.2.3 手工创建模式

##### 表单字段
| 字段名 | 组件类型 | 验证规则 | 默认值 |
|--------|----------|----------|--------|
| 任务名称 | Input | 必填，1-100字符 | - |
| 任务类型 | Select | 必填 | 大模型评测 |
| 优先级 | Select | 必填 | 中 |
| 描述 | Textarea | 选填，最多500字符 | - |

##### 界面交互
1. **表单填写**
   - 所有字段垂直排列
   - 必填字段带*号标识
   - 输入时实时校验，错误即时提示
   - 描述字段提供字数统计

2. **提交操作**
   - 底部"取消"和"创建"按钮
   - 点击"取消"关闭面板，不做任何操作
   - 点击"创建"：
     - 校验通过：创建任务，关闭面板，列表新增任务，显示成功提示
     - 校验失败：高亮错误字段，显示错误提示

3. **成功反馈**
   - 右上角弹出成功提示"任务创建成功"
   - 新任务出现在列表顶部

#### 2.2.4 自然语言创建模式

##### 功能描述
用户输入一段自然语言描述（如评测需求、测试场景等），系统通过AI理解并自动拆解成多个评测任务。

##### 界面交互
1. **输入区域**
   - 大文本框（Textarea），高度约200px
   - 占位提示："请输入您的评测需求描述，例如：评测GPT-4o的多轮对话能力、逻辑推理能力和代码生成能力..."
   - 右下角显示字数统计（最多2000字符）

2. **示例提示**
   - 输入框下方显示"示例"折叠区域
   - 展开后显示示例输入：
     ```
     需要评测GPT-4o模型的以下能力：
     1. 多轮对话的上下文理解
     2. 复杂数学问题的推理能力
     3. Python代码生成和debug能力
     优先级：高优先级
     ```

3. **拆解操作**
   - 输入框下方"智能拆解"按钮
   - 点击后：
     - 按钮显示加载状态（旋转图标 + "拆解中..."）
     - 调用AI接口进行拆解
     - 拆解成功：显示拆解结果预览
     - 拆解失败：显示错误提示

4. **拆解结果预览**
   - 卡片式展示每个拆解出的任务
   - 每个任务卡片显示：
     - 任务名称（可编辑）
     - 任务类型（可修改）
     - 优先级（可修改）
     - 描述（可编辑）
   - 每个卡片右上角"删除"按钮，可移除不需要的任务
   - 底部"添加任务"按钮，可手动补充任务

5. **确认创建**
   - 预览区域下方显示"取消"和"确认创建"按钮
   - 点击"确认创建"：
     - 批量创建所有预览中的任务
     - 显示成功提示"成功创建 N 个任务"
     - 关闭面板，列表更新

##### 拆解结果示例
```
输入：需要评测GPT-4o模型的多轮对话、逻辑推理和代码生成能力

拆解结果：
┌─────────────────────────────────────┐
│ 任务1：GPT-4o多轮对话能力评测    [删除] │
│ 类型：大模型评测                     │
│ 优先级：高                           │
│ 描述：评测GPT-4o在多轮对话场景下的...  │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 任务2：GPT-4o逻辑推理能力评测    [删除] │
│ 类型：大模型评测                     │
│ 优先级：高                           │
│ 描述：评测GPT-4o在复杂数学问题上的...  │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ 任务3：GPT-4o代码生成能力评测    [删除] │
│ 类型：大模型评测                     │
│ 优先级：高                           │
│ 描述：评测GPT-4o在Python代码生成...   │
└─────────────────────────────────────┘
              [+ 添加任务]
```

##### 错误处理
| 错误类型 | 提示信息 | 处理方式 |
|----------|----------|----------|
| 输入为空 | 请输入评测需求描述 | 禁用拆解按钮 |
| 输入过长 | 描述不能超过2000字符 | 限制输入 |
| 拆解失败 | 拆解失败，请重试或使用手工创建 | 显示重试按钮 |
| 网络错误 | 网络异常，请检查网络后重试 | 显示重试按钮 |

#### 2.2.5 验收标准

**手工创建模式**
- [ ] 点击"新建任务"按钮正确弹出面板
- [ ] 默认选中"手工创建"模式
- [ ] 表单字段验证规则正确执行
- [ ] 必填字段未填写时无法提交
- [ ] 提交成功后列表正确更新

**自然语言创建模式**
- [ ] 可正常切换到"自然语言创建"模式
- [ ] 输入框显示正确的占位提示
- [ ] 示例区域可展开/折叠
- [ ] 点击"智能拆解"显示加载状态
- [ ] 拆解成功后正确显示任务预览
- [ ] 预览任务可编辑、删除
- [ ] 可添加额外任务
- [ ] 确认创建后批量创建任务
- [ ] 显示正确的成功提示（显示创建数量）

**通用**
- [ ] 模式切换平滑无卡顿
- [ ] 取消操作正确关闭面板且不创建任务
- [ ] 错误提示友好明确

---

### 2.3 任务详情查看

#### 2.3.1 功能描述
查看任务的详细信息，包括所有字段和创建时间等。

#### 2.3.2 界面交互
1. **触发入口**
   - 点击任务卡片的"查看详情"按钮
   - 弹出抽屉式详情面板

2. **详情展示**
   - 标题显示任务名称
   - 主体区域展示所有字段信息
   - 信息使用描述列表形式展示（字段名：值）

3. **关闭操作**
   - 点击抽屉外部区域关闭
   - 点击右上角关闭按钮关闭

#### 2.3.3 验收标准
- [ ] 点击"查看详情"正确弹出详情面板
- [ ] 详情信息完整展示所有字段
- [ ] 关闭操作正常工作

---

## 3. 非功能需求

### 3.1 性能要求
- 页面首屏加载时间 < 2秒
- 列表渲染时间 < 500ms
- 搜索响应时间 < 200ms

### 3.2 兼容性要求
- 支持Chrome、Firefox、Edge最新版本
- 支持1920x1080及以上分辨率

### 3.3 易用性要求
- 界面简洁清晰，符合内部管理系统风格
- 操作流程直观，无需培训即可使用
- 错误提示友好，指导用户正确操作

---

## 4. 界面布局设计

### 4.1 整体布局
```
┌─────────────────────────────────────────────────────┐
│  Header: 评测任务管理系统          [新建任务] 按钮   │
├─────────────────────────────────────────────────────┤
│  [搜索框]                                            │
│  [全部] [待执行] [执行中] [已完成] [已失败]          │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │ 任务卡片 │  │ 任务卡片 │  │ 任务卡片 │             │
│  │         │  │         │  │         │             │
│  │ [查看详情]│  │ [查看详情]│  │ [查看详情]│             │
│  └─────────┘  └─────────┘  └─────────┘             │
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │ 任务卡片 │  │ 任务卡片 │  │ 任务卡片 │             │
│  │         │  │         │  │         │             │
│  │ [查看详情]│  │ [查看详情]│  │ [查看详情]│             │
│  └─────────┘  └─────────┘  └─────────┘             │
└─────────────────────────────────────────────────────┘
```

### 4.2 任务卡片布局
```
┌─────────────────────────────┐
│ 任务名称            [状态标签] │
├─────────────────────────────┤
│ 类型：大模型评测              │
│ 创建人：张三                  │
│ 创建时间：2024-01-15 10:30   │
│ 优先级：高                    │
├─────────────────────────────┤
│                    [查看详情] │
└─────────────────────────────┘
```

---

## 5. 技术实现说明

### 5.1 技术栈
- **框架**: Next.js 16 (App Router)
- **UI组件**: shadcn/ui
- **样式**: Tailwind CSS 4
- **类型**: TypeScript 5

### 5.2 数据管理
- 使用React State管理本地任务数据
- 任务数据存储在组件状态中（模拟本地存储）
- 后续可扩展接入后端API

### 5.3 组件划分
- `TaskListPage`: 任务列表主页面
- `TaskCard`: 任务卡片组件
- `TaskFilters`: 筛选标签页组件
- `CreateTaskSheet`: 新建任务抽屉（含模式切换）
- `ManualCreateForm`: 手工创建表单组件
- `NaturalLanguageCreate`: 自然语言创建组件
- `TaskPreviewCard`: 任务预览卡片组件（用于拆解结果）
- `TaskDetailSheet`: 任务详情抽屉

### 5.4 AI接口集成
- 自然语言拆解功能调用LLM接口
- 使用流式响应（SSE）提升用户体验
- 接口地址：`/api/task/parse`（待实现）

---

## 6. 测试用例

### 6.1 任务列表展示
| 用例ID | 用例名称 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC001 | 查看空列表 | 访问页面，无任务数据 | 显示"暂无任务数据"提示 |
| TC002 | 查看任务列表 | 访问页面，有多条任务 | 正确展示所有任务卡片 |
| TC003 | 状态筛选 | 点击"已完成"标签 | 仅显示状态为"已完成"的任务 |
| TC004 | 搜索功能 | 在搜索框输入关键词 | 列表实时过滤匹配的任务 |

### 6.2 新建任务
| 用例ID | 用例名称 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC005 | 打开新建表单 | 点击"新建任务"按钮 | 弹出新建任务抽屉 |
| TC006 | 表单验证 | 不填写必填项，点击创建 | 显示验证错误提示 |
| TC007 | 创建成功 | 填写完整信息，点击创建 | 创建成功，列表更新 |
| TC008 | 取消创建 | 填写部分信息，点击取消 | 关闭抽屉，不创建任务 |

---

## 7. 版本规划

### v1.0（当前版本）
- ✅ 任务列表展示
- ✅ 新建任务功能（手工创建）
- ✅ 任务详情查看
- ✅ 状态筛选
- ✅ 搜索功能

### v1.1（当前迭代）
- ✅ 新建任务模式选择（手工创建 / 自然语言创建）
- ✅ 自然语言拆解自动创建任务
- ✅ 拆解结果预览与编辑
- ✅ 批量创建任务

### v1.2（后续规划）
- [ ] 任务编辑功能
- [ ] 任务删除功能
- [ ] 批量操作
- [ ] 分页功能

### v2.0（未来规划）
- [ ] 接入后端API
- [ ] 任务执行日志
- [ ] 数据统计图表
- [ ] 导出功能
