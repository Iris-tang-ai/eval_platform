# 评测任务管理系统 PRD

## 1. 产品概述

### 1.1 产品定位
评测任务管理系统是一个内部使用的Web应用，用于管理和追踪评测任务的整个生命周期，提供任务创建、列表展示、状态追踪等核心功能。

### 1.2 目标用户
- 评测团队工程师
- 项目管理人员
- 质量保障团队

### 1.3 核心价值
- 统一管理评测任务，提升团队协作效率
- 清晰展示任务历史，便于回溯分析
- 规范化评测流程，确保质量可控

---

## 2. 功能需求

### 2.1 任务列表展示

#### 2.1.1 功能描述
展示所有评测任务的历史记录，支持查看任务详情、状态筛选和搜索。

#### 2.1.2 数据字段
| 字段名 | 类型 | 说明 | 是否必填 |
|--------|------|------|----------|
| 任务ID | string | 唯一标识符，自动生成 | 是 |
| 任务名称 | string | 任务描述性名称 | 是 |
| 任务类型 | enum | 大模型评测/图像评测/语音评测/视频评测 | 是 |
| 创建人 | string | 任务创建者姓名 | 是 |
| 创建时间 | datetime | 任务创建时间戳 | 是 |
| 状态 | enum | 待执行/执行中/已完成/已失败/已取消 | 是 |
| 优先级 | enum | 高/中/低 | 是 |
| 描述 | string | 任务详细描述 | 否 |

#### 2.1.3 界面交互
1. **列表展示**
   - 采用卡片式布局，每个任务一张卡片
   - 卡片头部显示任务名称和状态标签
   - 卡片主体显示任务类型、创建人、创建时间、优先级
   - 状态使用不同颜色标签区分：
     - 待执行：灰色
     - 执行中：蓝色
     - 已完成：绿色
     - 已失败：红色
     - 已取消：黄色

2. **状态筛选**
   - 顶部提供状态筛选标签页：全部/待执行/执行中/已完成/已失败
   - 点击标签页即时切换列表内容
   - 默认展示"全部"状态

3. **搜索功能**
   - 顶部搜索框支持按任务名称模糊搜索
   - 输入即时过滤，无需点击搜索按钮
   - 搜索框支持清空功能

4. **列表排序**
   - 默认按创建时间倒序排列（最新任务在前）
   - 支持按优先级、状态排序

#### 2.1.4 验收标准
- [ ] 任务列表正确展示所有历史任务
- [ ] 状态筛选功能正常，点击标签页能正确过滤
- [ ] 搜索功能正常，输入关键词能正确过滤
- [ ] 排序功能正常，默认按时间倒序
- [ ] 空状态友好提示"暂无任务数据"
- [ ] 任务卡片信息完整，无遗漏字段

---

### 2.2 新建任务

#### 2.2.1 功能描述
提供新建评测任务的表单，用户填写必要信息后创建任务。

#### 2.2.2 表单字段
| 字段名 | 组件类型 | 验证规则 | 默认值 |
|--------|----------|----------|--------|
| 任务名称 | Input | 必填，1-100字符 | - |
| 任务类型 | Select | 必填 | 大模型评测 |
| 优先级 | Select | 必填 | 中 |
| 描述 | Textarea | 选填，最多500字符 | - |

#### 2.2.3 界面交互
1. **触发入口**
   - 页面右上角"新建任务"按钮
   - 点击弹出抽屉式表单面板

2. **表单填写**
   - 所有字段垂直排列
   - 必填字段带*号标识
   - 输入时实时校验，错误即时提示
   - 描述字段提供字数统计

3. **提交操作**
   - 底部"取消"和"创建"按钮
   - 点击"取消"关闭抽屉，不做任何操作
   - 点击"创建"：
     - 校验通过：创建任务，关闭抽屉，列表新增任务，显示成功提示
     - 校验失败：高亮错误字段，显示错误提示

4. **成功反馈**
   - 右上角弹出成功提示"任务创建成功"
   - 新任务出现在列表顶部（因按时间倒序）

#### 2.2.4 验收标准
- [ ] 点击"新建任务"按钮正确弹出表单
- [ ] 表单字段验证规则正确执行
- [ ] 必填字段未填写时无法提交
- [ ] 描述字段字数统计准确
- [ ] 提交成功后列表正确更新
- [ ] 提交成功后显示成功提示
- [ ] 取消操作正确关闭抽屉且不创建任务

---

### 2.3 任务详情查看

#### 2.3.1 功能描述
查看任务的详细信息，包括所有字段和创建时间等。

#### 2.3.2 界面交互
1. **触发入口**
   - 点击任务卡片的"查看详情"按钮
   - 弹出抽屉式详情面板

2. **详情展示**
   - 标题显示任务名称
   - 主体区域展示所有字段信息
   - 信息使用描述列表形式展示（字段名：值）

3. **关闭操作**
   - 点击抽屉外部区域关闭
   - 点击右上角关闭按钮关闭

#### 2.3.3 验收标准
- [ ] 点击"查看详情"正确弹出详情面板
- [ ] 详情信息完整展示所有字段
- [ ] 关闭操作正常工作

---

## 3. 非功能需求

### 3.1 性能要求
- 页面首屏加载时间 < 2秒
- 列表渲染时间 < 500ms
- 搜索响应时间 < 200ms

### 3.2 兼容性要求
- 支持Chrome、Firefox、Edge最新版本
- 支持1920x1080及以上分辨率

### 3.3 易用性要求
- 界面简洁清晰，符合内部管理系统风格
- 操作流程直观，无需培训即可使用
- 错误提示友好，指导用户正确操作

---

## 4. 界面布局设计

### 4.1 整体布局
```
┌─────────────────────────────────────────────────────┐
│  Header: 评测任务管理系统          [新建任务] 按钮   │
├─────────────────────────────────────────────────────┤
│  [搜索框]                                            │
│  [全部] [待执行] [执行中] [已完成] [已失败]          │
├─────────────────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │ 任务卡片 │  │ 任务卡片 │  │ 任务卡片 │             │
│  │         │  │         │  │         │             │
│  │ [查看详情]│  │ [查看详情]│  │ [查看详情]│             │
│  └─────────┘  └─────────┘  └─────────┘             │
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │ 任务卡片 │  │ 任务卡片 │  │ 任务卡片 │             │
│  │         │  │         │  │         │             │
│  │ [查看详情]│  │ [查看详情]│  │ [查看详情]│             │
│  └─────────┘  └─────────┘  └─────────┘             │
└─────────────────────────────────────────────────────┘
```

### 4.2 任务卡片布局
```
┌─────────────────────────────┐
│ 任务名称            [状态标签] │
├─────────────────────────────┤
│ 类型：大模型评测              │
│ 创建人：张三                  │
│ 创建时间：2024-01-15 10:30   │
│ 优先级：高                    │
├─────────────────────────────┤
│                    [查看详情] │
└─────────────────────────────┘
```

---

## 5. 技术实现说明

### 5.1 技术栈
- **框架**: Next.js 16 (App Router)
- **UI组件**: shadcn/ui
- **样式**: Tailwind CSS 4
- **类型**: TypeScript 5

### 5.2 数据管理
- 使用React State管理本地任务数据
- 任务数据存储在组件状态中（模拟本地存储）
- 后续可扩展接入后端API

### 5.3 组件划分
- `TaskListPage`: 任务列表主页面
- `TaskCard`: 任务卡片组件
- `TaskFilters`: 筛选标签页组件
- `TaskSearchBar`: 搜索栏组件
- `CreateTaskSheet`: 新建任务抽屉
- `TaskDetailSheet`: 任务详情抽屉

---

## 6. 测试用例

### 6.1 任务列表展示
| 用例ID | 用例名称 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC001 | 查看空列表 | 访问页面，无任务数据 | 显示"暂无任务数据"提示 |
| TC002 | 查看任务列表 | 访问页面，有多条任务 | 正确展示所有任务卡片 |
| TC003 | 状态筛选 | 点击"已完成"标签 | 仅显示状态为"已完成"的任务 |
| TC004 | 搜索功能 | 在搜索框输入关键词 | 列表实时过滤匹配的任务 |

### 6.2 新建任务
| 用例ID | 用例名称 | 操作步骤 | 预期结果 |
|--------|----------|----------|----------|
| TC005 | 打开新建表单 | 点击"新建任务"按钮 | 弹出新建任务抽屉 |
| TC006 | 表单验证 | 不填写必填项，点击创建 | 显示验证错误提示 |
| TC007 | 创建成功 | 填写完整信息，点击创建 | 创建成功，列表更新 |
| TC008 | 取消创建 | 填写部分信息，点击取消 | 关闭抽屉，不创建任务 |

---

## 7. 版本规划

### v1.0（当前版本）
- ✅ 任务列表展示
- ✅ 新建任务功能
- ✅ 任务详情查看
- ✅ 状态筛选
- ✅ 搜索功能

### v1.1（后续规划）
- [ ] 任务编辑功能
- [ ] 任务删除功能
- [ ] 批量操作
- [ ] 分页功能

### v2.0（未来规划）
- [ ] 接入后端API
- [ ] 任务执行日志
- [ ] 数据统计图表
- [ ] 导出功能
